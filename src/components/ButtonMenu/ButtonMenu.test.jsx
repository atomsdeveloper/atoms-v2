import { renderTheme } from '../../styles/render-theme';
import { ButtonMenu } from '.';

describe('<ButtonMenu />', () => {
    it('should render', () => {
        renderTheme(<ButtonMenu />);
    });
})